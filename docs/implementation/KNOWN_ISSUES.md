# Known Issues

No open source-code, lint, type, test, migration or production-build failure remains in Phase 0.
The following environment/provider limitations are recorded rather than represented as completed
production integrations.

| Open issue                                                                      | Severity | Impact                                                                                     | Reproduction or evidence                                                                                                                                                                                        | Workaround                                                                                                                     | Owning phase                          | Resolution status      |
| ------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------- | ---------------------- |
| Docker/Compose was unavailable on the verification host                         | Low      | The exact `pnpm services:up` and Docker CLI workflow could not be executed on this machine | `docker --version` was unavailable. Equivalent PostgreSQL 17 and Redis 8 containers were started with rootless Podman, the migration and API readiness transitions passed, and Compose image tags were resolved | Install Docker with Compose and run the documented root commands; Podman can exercise the same images manually                 | Phase 0 environment                   | Open, environment-only |
| Hosted Supabase, Upstash and Tigris credentials were not used                   | Low      | Provider networking, TLS and account policy remain unproved until a staging deployment     | Completion used real local PostgreSQL/Redis and the provider-neutral PostgreSQL, Redis and S3-compatible interfaces                                                                                             | Supply staging credentials through the documented server-only environment variables and run readiness/storage smoke checks     | Deployment / Phase 14                 | Deferred               |
| No physical Android device/emulator or signed production artifact was available | Low      | On-device rendering, remote FCM delivery and store signing are not yet evidenced           | Metro start, Expo prebuild, dependency validation, production Android export and Gradle release-manifest processing passed; FCM credentials were intentionally absent                                           | Run the shell on a device/emulator during Phase 1 and configure reviewed FCM/EAS signing credentials before production release | Authentication mobile work / Phase 14 | Deferred               |
