# Known Issues

No source formatting, lint, type-check, unit/integration test, OpenNext build/preview, migration
metadata, monorepo build or API image-build failure remains in the Phase 0 deployment amendment.

| Open issue                                                                                                    | Severity | Impact                                                                                                      | Reproduction or evidence                                                                                                                                                                   | Workaround                                                                                                                                                | Owning phase                               | Resolution status      |
| ------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | ---------------------- |
| Hosted Cloudflare, Render, Supabase, Upstash and Tigris/R2 staging smoke has not run                          | Medium   | Provider DNS, TLS, quotas, account policy, CORS and secret wiring remain unproved outside local equivalents | No hosted credentials were supplied and no external deployment was authorized. OpenNext local preview, Render schema audit, PostgreSQL 17, Redis 8 and the production image passed locally | Provision a staging stack, apply migrations once, set every secret, deploy Render first, then Cloudflare, and run the smoke order in `DEPLOYMENT.md`      | Deployment / Phase 14                      | Deferred               |
| Docker CLI/Compose is unavailable on this verification host                                                   | Low      | Exact Docker Engine behavior and `pnpm services:up` were not re-run here                                    | `docker` is absent; Podman built the unchanged Dockerfile and ran both API and worker from the resulting image                                                                             | CI executes `docker build`; also run the documented Docker command on a Docker-equipped staging host                                                      | Phase 0 environment                        | Open, environment-only |
| Standalone worker failures currently report through structured Pino logs only                                 | Low      | Worker exceptions are visible in Render logs but are not sent through the API's Sentry reporter adapter     | `WorkerModule` configures Pino and does not import the HTTP observability module; `render.yaml` therefore does not inject `SENTRY_DSN` into the worker                                     | Retain Render log alerts; add a worker-specific error-reporter boundary in a later observability/release phase before business processors become critical | Background processing / Phase 14           | Deferred               |
| Internal `HttpException` messages with a 5xx status are not forcibly sanitized by the pre-existing API filter | Medium   | A future route that throws a 5xx `HttpException` containing provider/internal detail could return that text | `payloadFor` uses an explicit `HttpException` response message even when its status is 500+; current public health paths sanitize dependency failures and no business routes exist         | Do not place internal/provider detail in `HttpException` messages; harden the filter and add an integration test before Phase 1 exposes protected routes  | Foundation security / Phase 1 prerequisite | Open                   |
| No physical Android device/emulator or signed production artifact was available                               | Low      | On-device rendering, remote notification delivery and store signing remain unproved                         | Phase 0 previously passed Metro, Expo prebuild/export and Android manifest checks without production FCM/signing credentials                                                               | Run device/emulator checks during Phase 1 and configure reviewed FCM/EAS signing before release                                                           | Mobile auth / Phase 14                     | Deferred               |
