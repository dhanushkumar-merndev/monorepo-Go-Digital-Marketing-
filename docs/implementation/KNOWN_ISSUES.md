# Known Issues

The Phase 14 source audit has no known Critical/High code blocker for the local prototype. The
production release remains NO-GO until the external evidence below is supplied. Development
adapters and unavailable providers remain visibly fail-closed.

| Open issue                                                                                       | Severity | Impact                                                                                                       | Reproduction or evidence                                                                                                                                                                                | Workaround                                                                                                                                                                                     | Owning phase             | Resolution status            |
| ------------------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ---------------------------- |
| Hosted Supabase migration, PITR and isolated restore are not verified                            | Medium   | Production recovery objectives are not proven                                                                | Local zero-to-latest, populated compatibility and PGlite snapshot/restore tests pass; no shared database was authorized                                                                                 | Take a recovery point, apply one migration job, restore into isolation and attach evidence before promotion                                                                                    | Phase 14 release         | Open external gate           |
| Linux release artifacts and container evidence are not available on this Windows host            | Medium   | Cloudflare bundle, non-root Docker smoke, SBOM and image scan are not release-proven                         | Next/OpenNext reaches bundle generation but Windows symlink creation fails with `EPERM`; Docker/working WSL is unavailable                                                                              | Use the checked-in Linux CI workflow and retain artifact digests and scan output                                                                                                               | Phase 14 deployment      | Open environment gate        |
| Official provider credentials, callbacks and approvals were not supplied                         | Medium   | Telephony, messaging, social and AI integrations cannot be called production-ready                           | Development/unavailable adapters fail visibly; no official-provider evidence exists                                                                                                                     | Configure encrypted tenant credentials in staging, verify signatures, reconciliation, consent and provider outage paths                                                                        | Phase 13/14 integrations | Open external gate           |
| No approved malware scanner produces real `CLEAN` evidence                                       | Medium   | Commercial, delivery, registration and messaging media remain unavailable after upload                       | Download/use paths now require verified clean state and fail closed                                                                                                                                     | Bind an approved tenant-safe scanner/quarantine adapter and test clean, rejected and unavailable paths                                                                                         | Storage security         | Open external gate           |
| Signed mobile artifacts and physical-device workflows are not verified                           | Medium   | Runtime permissions, notifications, active-job location and offline replay are not device-proven             | Android/iOS production JS exports pass; no signing credentials or devices were supplied                                                                                                                 | Run the signed-device matrix in `runbooks/MOBILE_RELEASE.md` before pilot                                                                                                                      | Phase 14 mobile release  | Open external gate           |
| Load, soak, alert routing, legal approval and dealership pilot are pending                       | Medium   | Operational capacity and business acceptance are unknown                                                     | Tools and runbooks exist but no approved target, named owners or signatories were supplied                                                                                                              | Execute the release evidence and pilot runbooks in staging, then validate the completed evidence file                                                                                          | Phase 14 operations      | Open external gate           |
| Raw workspace production audit reports two `image-size` advisories in the Expo/Metro build graph | Medium   | The broad workspace audit is not advisory-free, although the pruned API runtime does not contain the package | `pnpm audit --prod --audit-level high --json` reports `GHSA-w3rx-r6r6-pgpr` and `GHSA-5p2g-fcmc-qvqq`; Metro resolves `image-size@1.2.1` and the release audit verifies absence from the pruned runtime | Keep exact advisory exceptions release-scoped, update when the Expo/Metro graph safely accepts the patched major, and fail CI if either enters the shipped API graph or a new advisory appears | Phase 14 supply chain    | Open dependency risk, scoped |
| Application-layer encryption is not implemented for the minimized mobile SQLite command outbox   | Medium   | A compromised unlocked device could expose queued follow-up/note payloads                                    | Tokens stay in SecureStore and documents/credentials are never queued, but command JSON is plain SQLite                                                                                                 | Enforce device security and session revocation; approve encrypted local storage before expanding offline data                                                                                  | Mobile security          | Accepted documented risk     |

No external gate above is represented as locally passed. Exact command results are tracked in
`PHASE_STATUS.md`; operational acceptance must be recorded in `RELEASE_EVIDENCE_TEMPLATE.md`.
